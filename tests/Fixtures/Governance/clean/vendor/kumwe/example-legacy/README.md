# Kumwe Example Legacy

Fictitious package released before the Version 2 protocol, used by the governance fixture as an approved
legacy-unmanifested entry. It ships no manifests and no handoff; its public symbols are found by a source
scan of `src/` that excludes `@internal` declarations.

## Responsibility

Format legacy descriptions for the example module. It does not own persistence, authorization or
presentation.
